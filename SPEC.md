# Spec — เว็บแอประบบแจก e-Token (TFT)

สรุปสเปกทางเทคนิคจากขอบเขตงานใน [SCOPE.md](SCOPE.md) — ยังไม่ได้ลงมือสร้างจริง เอกสารนี้ไว้ให้ตรวจสอบก่อน

## บทบาทผู้ใช้ (Roles)

มี 3 บทบาท เก็บเป็นฟิลด์ `role` ในบัญชีผู้ใช้แต่ละคน:

| บทบาท | ทำอะไรได้ |
|---|---|
| `participant` (ผู้เข้าร่วม) | ดูยอด token สะสมของตัวเอง + ประวัติแจก/หักทั้งหมด |
| `trainer` (วิทยากร/ผู้ดูแลกลุ่ม) | แจก token ให้ผู้เข้าร่วมคนใดก็ได้ (หักจากโควตาของตัวเอง) + ดูประวัติที่ตัวเองแจกไป |
| `admin` (แอดมิน) | ทำได้ทุกอย่างของ trainer ไม่ได้ (ไม่มีโควตา ไม่แจกเอง) แต่คุมภาพรวมทั้งหมด: ตั้งค่าโควตาวิทยากร, หัก token คืนประมูล, แก้ไข/ยกเลิกรายการ, จัดการบัญชี, export ข้อมูล — **รวมบทบาท "เจ้าหน้าที่คืนประมูล" ไว้ในนี้แล้ว ไม่มีบทบาทแยก** |

หนึ่งบัญชีมีได้แค่บทบาทเดียว (ไม่มี multi-role ในรอบนี้)

## หน้าจอ (Screens)

**ใช้ร่วมกันทุกบทบาท**
- `Login` — กรอก username/password
- `ChangePassword` — เปลี่ยนรหัสผ่านจากรหัสชั่วคราวเป็นของตัวเอง (ไม่บังคับ)

**ผู้เข้าร่วม (`participant`)**
- `ParticipantDashboard` — ยอด token รวม + รายการประวัติแจก/หักทั้งหมดที่เกิดกับตัวเอง เรียงใหม่→เก่า

**วิทยากร (`trainer`)**
- `GrantToken` — กรอกรหัสประจำตัวผู้รับ → เลือกจำนวน (ปุ่ม 1/3/5/10/15 หรือระบุเอง) → เลือกเหตุผล (ปุ่ม "ตอบคำถาม"/"ร่วมกิจกรรม" หรือพิมพ์เอง) → ยืนยัน — หน้านี้แสดงโควตาคงเหลือของตัวเองด้วยตลอดเวลา
- `TrainerHistory` — ประวัติรายการที่ตัวเองแจกไปทั้งหมด

**แอดมิน (`admin`)**
- `AdminOverview` — ภาพรวมยอด token ของผู้เข้าร่วมทุกคน + โควตาคงเหลือของวิทยากรทุกคน
- `TrainerQuotaSettings` — ปรับโควตา token ของวิทยากรแต่ละคน (ค่าเริ่มต้น 50)
- `DeductToken` — กรอกรหัสประจำตัวผู้ชนะประมูล → ระบุจำนวนที่หัก + รายการของรางวัลที่แลกไป → ยืนยัน
- `TransactionEditor` — ค้นหารายการแจก/หักที่ผ่านมา → แก้ไขหรือยกเลิก (voided ไม่ใช่ลบทิ้ง)
- `UserManagement` — สร้าง/รีเซ็ตบัญชีผู้ใช้ (username `tft01`–`tft80`, สุ่มรหัสผ่านชั่วคราว), พิมพ์สลิปรหัสผ่าน
- `ExportData` — export ยอด/ประวัติทั้งหมดเป็น CSV

## โครงสร้างข้อมูล (Firestore collections)

**`users/{uid}`** — บัญชีผู้ใช้ทุกบทบาท
| ฟิลด์ | ชนิด | หมายเหตุ |
|---|---|---|
| `username` | string | pattern `tft01`–`tft80` |
| `fullName` | string | ชื่อ-นามสกุล |
| `role` | string | `participant` \| `trainer` \| `admin` |
| `tokenBalance` | number | เฉพาะ `participant` — ยอดสะสมปัจจุบัน (denormalized เพื่ออ่านเร็ว อัปเดตพร้อม transaction ทุกครั้ง) |
| `tokenQuota` | number | เฉพาะ `trainer` — โควตาคงเหลือ ค่าเริ่มต้น `50` |
| `mustChangePassword` | boolean | true จนกว่าจะเปลี่ยนรหัสผ่านครั้งแรก |
| `createdAt` | timestamp | |

**`transactions/{id}`** — ทุกการเคลื่อนไหวของ token (audit trail หลัก)
| ฟิลด์ | ชนิด | หมายเหตุ |
|---|---|---|
| `type` | string | `grant` (แจก) \| `deduct` (หัก) \| `quotaAdjust` (แอดมินปรับโควตาวิทยากร) |
| `actorUid` | string | ผู้ทำรายการ (วิทยากรที่แจก / แอดมินที่หักหรือปรับโควตา) |
| `targetUid` | string | ผู้เข้าร่วมที่ได้รับผลกระทบยอด (ไม่มีสำหรับ `quotaAdjust` — ใช้ `trainerUid` แทน) |
| `trainerUid` | string | เฉพาะ `quotaAdjust` — วิทยากรที่ถูกปรับโควตา |
| `amount` | number | จำนวน token เป็นค่าบวกเสมอ ทิศทางดูจาก `type` |
| `reason` | string | ข้อความเหตุผล (สำหรับ `grant`) |
| `reasonSource` | string | `preset` \| `custom` |
| `itemLabel` | string | เฉพาะ `deduct` — ชื่อรางวัลที่แลก |
| `status` | string | `active` \| `voided` |
| `voidedBy`, `voidedAt`, `voidReason` | — | กรอกเมื่อแอดมินยกเลิกรายการ |
| `createdAt` | timestamp | |

ไม่มีการลบเอกสารจริงในคอลเลกชันนี้ — ยกเลิกใช้ `status: voided` เท่านั้น เพื่อให้ audit trail สมบูรณ์ตลอด

## สิ่งที่ไม่ทำใน Module นี้ (Out of scope)

- ไม่เชื่อมกับ [auction-app](auction-app/index.html) แบบอัตโนมัติ — แอดมินหักยอดด้วยมือผ่าน `DeductToken` แทน
- ไม่มีบทบาท "เจ้าหน้าที่" แยกจากแอดมิน
- ไม่จำกัดสิทธิ์วิทยากรตามกลุ่ม/สาขา — แจกให้ผู้เข้าร่วมคนไหนก็ได้
- ไม่ทำระบบสมัครสมาชิก/ลงทะเบียนผู้ใช้ใหม่ด้วยตัวเอง (บัญชีทั้งหมดแอดมินสร้างล่วงหน้าเท่านั้น)
- ไม่ทำระบบจ่ายเงิน/แลกของรางวัลจริงโดยตรง (เป็นแค่บันทึกยอด token)
- ไม่ทำการยืนยันตัวตนขั้นสูง (2FA, rate-limit เข้มงวด, กู้คืนรหัสผ่านอัตโนมัติ)
- ไม่ทำแจ้งเตือนแบบ push/LINE Notify
- ไม่ทำแอปมือถือแยก (ใช้เว็บ responsive เดียว)
- ไม่ทำ multi-role ต่อหนึ่งบัญชี
