# Requirements — คู่มือกิจกรรม Training for Trainers (TFT)

เอกสารนี้อธิบายขอบเขตและแผนงานระยะยาวของคู่มือ เพื่อให้ทำงานต่อได้โดยไม่ต้องอธิบายบริบทซ้ำในแต่ละครั้ง

## เป้าหมายของคู่มือ

เว็บ/เอกสารข้อมูลสำหรับ **ผู้เข้าร่วม** กิจกรรม "วิทยากรเพื่อการพัฒนานักวิจัย" (Training for the Trainers) จัดโดย สำนักงานการวิจัยแห่งชาติ (วช.) ร่วมกับมหาวิทยาลัยแม่ฟ้าหลวง ระหว่างวันที่ 28 กันยายน – 2 ตุลาคม 2569

ไม่ใช่เอกสารรับสมัคร/ประชาสัมพันธ์ก่อนสมัคร (ปิดรับสมัครแล้ว) — เน้นข้อมูลที่ผู้ได้รับคัดเลือกแล้วต้องใช้ ตั้งแต่ก่อนเดินทางจนจบกิจกรรม

## หลักการจัดโครงสร้างเนื้อหา

**แบ่งตามช่วงเวลาที่ผู้เข้าร่วมต้องใช้ข้อมูล** ไม่ใช่แบ่งตามประเภทเอกสารดิบที่ได้รับมา:

| ไฟล์ | ช่วงเวลาที่ใช้ | เนื้อหา |
|---|---|---|
| `docs/00-introduction.md` | ก่อนเริ่มอ่านคู่มือ | ภาพรวมโครงการ วัตถุประสงค์ กลุ่มเป้าหมาย 7 กลุ่มสาขา |
| `docs/01-preparation.md` | **ก่อนเดินทาง** | ทุกอย่างที่ต้องรู้/ทำ ก่อนวันอบรม: ปฐมนิเทศ, checklist ของที่ต้องเตรียม, สถานที่/ที่พัก, ค่าใช้จ่ายที่ต้องรับผิดชอบเอง |
| `docs/02-agenda.md` | ระหว่างอบรม | กำหนดการรายวันแบบละเอียด + วิทยากรที่ปรึกษาประจำกลุ่ม |
| `docs/03-materials.md` | ระหว่าง/หลังอบรม | เอกสารประกอบแต่ละหมวด, pretest, posttest |
| `docs/04-trainers.md` | ระหว่าง/หลังอบรม | เลขประจำตัว Trainers, รายชื่อกลุ่ม workshop |
| `docs/05-announcements.md` | ตลอดโครงการ | ประกาศ/การเปลี่ยนแปลงล่าสุด เรียงใหม่→เก่า |
| `docs/06-contact.md` | ตลอดโครงการ | ช่องทางติดต่อผู้ประสานงาน + FAQ |

**กฎการจัดวางเนื้อหาใหม่:** เมื่อได้ข้อมูลใหม่มา ให้ถามตัวเองก่อนว่า "ผู้เข้าร่วมต้องรู้เรื่องนี้ตอนไหน" แล้ววางในไฟล์ตามช่วงเวลานั้น ไม่ใช่สร้างไฟล์ใหม่ตามรูปแบบเอกสารต้นทาง (เช่น ถ้าเป็นโปสเตอร์ประชาสัมพันธ์ ให้แยกเนื้อหาแต่ละส่วนไปลงไฟล์ตามช่วงเวลาที่เกี่ยวข้อง ไม่ใช่สร้าง "docs/poster.md")

## ส่วนที่ยังไม่มีข้อมูล — รอ input เพิ่มเติมในอนาคต

รายการนี้เป็นของที่**คาดว่าจะได้รับเพิ่มทีหลัง** ไม่ต้องถามผู้ใช้ซ้ำว่าคู่มือควรมีอะไรบ้าง — เติมตามรายการนี้เมื่อข้อมูลมาถึง:

- [x] แผนที่/พิกัด → `docs/01-preparation.md` (เติมแล้ว: ลิงก์ Google Maps)
- [x] ~~รายละเอียดการเดินทางมาสถานที่จัดงาน~~ → **ไม่ต้องรอข้อมูลนี้แล้ว** ผู้เข้าร่วมเดินทางมาเอง ไม่มีแผนจะจัดรถ/คำแนะนำเส้นทางแบบละเอียดจากผู้จัด — ถ้ามีอัปเดตจะแจ้งผ่านกลุ่มสื่อสารออนไลน์แทน (ดูข้อถัดไป) ไม่ต้องถามซ้ำเรื่องนี้อีก
- [x] ลิงก์กลุ่มสื่อสารออนไลน์ (กลุ่มรวม) → `docs/01-preparation.md` (เติมแล้ว: กลุ่ม LINE https://line.me/R/ti/g/HJtGkLN7G2)
- [ ] ลิงก์กลุ่มย่อยแยกตามสาขา (7 กลุ่ม) → `docs/01-preparation.md`
- [ ] เอกสารที่ต้องเตรียม/นำมาแสดง และ dress code → `docs/01-preparation.md`
- [ ] เอกสารประกอบการอบรมแต่ละหมวด (หมวดที่ 1–7) → `docs/03-materials.md`
- [ ] ลิงก์ Pretest / Posttest → `docs/03-materials.md`
- [ ] เลขประจำตัว Trainers รายบุคคล → `docs/04-trainers.md` (**ตัดสินใจแล้ว:** แสดงเป็นตารางเปิดสาธารณะ ไม่ทำพอร์ทัลล็อกอินส่วนตัว — ผู้เข้าร่วมทุกคนเห็นชื่อ/เลข/กลุ่มของกันและกันได้)
- [ ] รายชื่อสมาชิกแต่ละกลุ่ม workshop (7 กลุ่ม) → `docs/04-trainers.md` (ตารางเปิดสาธารณะเช่นกัน)
- [ ] FAQ → `docs/06-contact.md`

## หมายเหตุเกี่ยวกับ .claude/commands ที่มีอยู่เดิม

โฟลเดอร์นี้มี `.claude/commands/*.md` หลงเหลือมาจากโปรเจกต์อื่น (`contract70-guide` — คู่มือสัญญาทุนวิจัย, ระบบ index.html + GAS portal ส่วนตัว, deploy ผ่าน Netlify) แยกตามความเกี่ยวข้อง:

- **ใช้ไม่ได้เลย** (hardcode remote/ID เฉพาะโปรเจกต์เก่า): `deploy.md`, `gas-deploy.md`, `edit-handbook.md`, `sync-content.md`
- **`new-research-portal.md`** เป็น checklist/แม่แบบสถาปัตยกรรมทั่วไปสำหรับทำ "Handbook เว็บสาธารณะ + GAS portal ส่วนตัวที่ล็อกอินด้วยอีเมล มฟล." — ถามผู้ใช้แล้ว **ตัดสินใจไม่ใช้กับ TFT** (เลือกตารางสาธารณะใน `docs/04-trainers.md` แทน) เก็บไว้เป็นข้อมูลอ้างอิงเฉย ๆ เผื่ออนาคตเปลี่ยนใจอยากทำพอร์ทัลส่วนตัว — **อย่าเสนอพอร์ทัลซ้ำโดยไม่มีเหตุเปลี่ยนแปลง** (เช่น ผู้ใช้เปลี่ยนใจเอง หรือมีข้อมูลอ่อนไหวที่ไม่ควรเปิดสาธารณะเพิ่มเข้ามา)

## Design system

[DESIGN.md](DESIGN.md) — ระบบดีไซน์สำหรับหน้าเว็บคู่มือ (ปรับจากแนว Red Broadcast: เน้นความโค้งมนของทุกองค์ประกอบ + ฟอนต์ Prompt ทั้งเว็บ) ใช้ไฟล์นี้เป็นอ้างอิงเดียวเวลาสร้าง/แก้ไข UI ของหน้าเว็บ อย่าเดาสี/ฟอนต์/รัศมีขอบเอง

เมื่อสร้างหน้าเว็บจริง (เช่น `index.html`): แสดงเฉพาะ section ที่มีข้อมูลจริงแล้ว (เกี่ยวกับ, เตรียมตัว, กำหนดการ, ติดต่อ) — **ซ่อน** เอกสารการอบรม/ข้อมูลผู้เข้าอบรม (03, 04) ไว้ก่อนจนกว่าจะมีเนื้อหาจริงตามรายการใน "ส่วนที่ยังไม่มีข้อมูล" ด้านบน

**ประกาศสำคัญ (05) ก็ถูกซ่อนไว้เช่นกัน** (ผู้ใช้ขอให้ซ่อนเพราะยังไม่มีประกาศจริงที่ควรแจ้ง — เนื้อหาเดิม 2 รายการยังอยู่ใน `docs/05-announcements.md` และถูก comment ไว้ใน `index.html` ไม่ได้ลบ) เปิดใช้ section นี้อีกครั้งเมื่อมีประกาศจริงที่ต้องแจ้งผู้เข้าอบรม (ไม่ใช่แค่ข้อมูลทั่วไปที่มีอยู่แล้วในหน้าอื่น)

## หน้าเว็บจริง (`index.html`)

สร้างแล้วตาม DESIGN.md — single-page, ไม่มี build step, ใช้ vanilla HTML/CSS/JS ตรง ๆ (ไม่มี framework) ประกอบด้วย: hero, เกี่ยวกับ, เตรียมตัว, กำหนดการ (chip tabs สลับปฐมนิเทศ/วันที่ 1–5 + ตารางวิทยากรที่ปรึกษา), ประกาศ, ติดต่อ, ปุ่มสลับ dark/light mode (จำค่าไว้ผ่าน `localStorage` แบบ try/catch เผื่อ origin ที่บล็อก storage)

**docs/*.md ยังเป็น source of truth ของเนื้อหา** — `index.html` เป็นตัวแสดงผลที่ต้องอัปเดตตามด้วยมือทุกครั้งที่แก้ `docs/*.md` (ยังไม่มีสคริปต์ sync อัตโนมัติแบบโปรเจกต์เก่า) เวลาเติมข้อมูลใหม่ (จากเช็คลิสต์ "ส่วนที่ยังไม่มีข้อมูล" ด้านบน) ต้องแก้ทั้งสองที่: ไฟล์ `docs/` ที่เกี่ยวข้อง และ section ที่ตรงกันใน `index.html`

ทดสอบผ่านเบราว์เซอร์แล้ว (local http server): chip tab สลับวันทำงานถูกต้อง, ปุ่ม dark/light mode ทำงานถูกต้อง (ต้อง wrap `localStorage` ด้วย try/catch — บาง origin เช่น sandboxed preview บล็อก storage ทำให้ script ทั้งไฟล์ throw ถ้าไม่ wrap), ฟอนต์ Prompt render ภาษาไทยถูกต้องทั้ง light/dark

## รูปภาพ (`assets/images/`)

โฟลเดอร์เก็บไฟล์รูป (เช่น QR code กลุ่ม LINE, โลโก้, ภาพประกอบอื่น ๆ) ใช้ path แบบ `assets/images/ชื่อไฟล์.png` อ้างอิงใน `index.html` ด้วย `<img src="assets/images/ชื่อไฟล์.png">`

**วิธีส่งรูปให้ใส่ในเว็บ:** รูปที่วางแปะ/paste ลงแชทโดยตรงเป็นแค่ inline preview เอาไฟล์จริงไม่ได้ ต้องทำอย่างใดอย่างหนึ่ง:
1. ลากไฟล์รูป (drag & drop) เข้าไปในหน้าต่างแชท Claude Code แทนการ paste, หรือ
2. บอก path ไฟล์บนเครื่อง (เช่น `C:\Users\Lenovo\Downloads\line-qr.png`) แล้วให้ก็อปปี้เข้า repo ให้

## Git / Deploy

- Local repo init แล้ว, remote `origin` → `git@github.com:mfu-rd/Training_for_Trainers_handbook.git`, SSH key ตั้งค่าและใช้งานได้แล้ว
- **Deploy: ย้ายจาก Netlify มาเป็น GitHub Pages แล้ว** (ตัดสินใจ 2569-08-25 หลัง Netlify credit ของทีม มฟล. หมดกลางทาง) — เหตุผล: GitHub Pages ฟรีไม่มีระบบเครดิตหมดอายุแบบ Netlify
  - Repo เปลี่ยนเป็น **Public** แล้ว (ผู้ใช้ยืนยันว่าเนื้อหาไม่มีอะไรลับ) — ทำเองผ่าน GitHub Settings → Danger Zone → Change visibility (ผมทำแทนไม่ได้ ไม่มี GitHub API token)
  - เปิด GitHub Pages เองผ่าน Settings → Pages → Source: Deploy from branch → Branch `master` / folder `/ (root)` (ผมทำแทนไม่ได้เช่นกัน)
  - **ย้าย repo ไปอยู่ใต้ GitHub Organization แล้ว** เพื่อไม่ให้ URL ติดชื่อบัญชีส่วนตัว — ตั้งใจจะใช้ชื่อ `rd-mfu` แต่ชื่อซ้ำ เลยได้ org จริงชื่อ **`mfu-rd`** แทน (org "belongs to: a business/institution" คือ ม.แม่ฟ้าหลวง เผื่ออนาคตเก็บ repo อื่นของหน่วยงานไว้ที่เดียวกันได้) — ใช้วิธี GitHub "Transfer ownership" ไม่ใช่สร้าง repo ใหม่ ประวัติ commit ทั้งหมดติดไปด้วย, GitHub ตั้ง redirect จาก URL เก่าให้อัตโนมัติ
  - URL ใหม่: `https://mfu-rd.github.io/Training_for_Trainers_handbook/` (แทนที่ลิงก์ Netlify เดิมทั้งหมด — ผู้ใช้ยืนยันว่ายังไม่เคยแจกลิงก์ Netlify ให้ใครไปจริงจัง เลยเปลี่ยนได้โดยไม่มีปัญหา)
  - เพิ่มไฟล์ `.nojekyll` ที่ root แล้ว (ป้องกัน GitHub รัน Jekyll processing ใส่ static site ของเราโดยไม่ตั้งใจ)
  - **ของที่ต้องเตือนผู้ใช้:** ควรไปลบ/ยกเลิกการเชื่อม site บน Netlify ด้วย (ที่ app.netlify.com) ไม่งั้นพอรอบเครดิตใหม่มา (~24-25 ก.ย.) มันจะกลับมา auto-deploy ทุกครั้งที่ push อีก แล้วแอบกินเครดิตของทีมต่อทั้ง ๆ ที่ไม่ได้ใช้แล้ว (ผู้ใช้กด "disable" project บน Netlify ไว้แล้วแทนการลบถาวร — เพียงพอแล้ว)
- ไม่มีข้อจำกัดเรื่อง credit/เครดิตหมดอีกต่อไปหลังย้ายมา GitHub Pages — **ไม่ต้องรอ confirm ก่อน push เพราะเรื่องเครดิตแล้ว** (กฎ "commit ก่อน push ทีหลัง" ที่ตั้งไว้ตอนใช้ Netlify ไม่มีเหตุผลรองรับอีกต่อไปสำหรับเหตุผลด้านเครดิต — แต่ยังเป็นนิสัยที่ดีทั่วไปที่จะ confirm การเปลี่ยนแปลงใหญ่ก่อน push อยู่ดี)
