# Scope — เว็บแอประบบแจก e-Token (TFT)

ร่างขอบเขตงานสำหรับเว็บแอปแจก token แบบออนไลน์ ของโครงการ Training for the Trainers (TFT) จัดระหว่าง 28 ก.ย. – 2 ต.ค. 2569

## ที่มา / ทำไมต้องทำ

กลไกเดิม: วิทยากรแจก token/เหรียญ/สติกเกอร์ **ด้วยมือ** ให้ผู้เข้าร่วมระหว่างบรรยาย/กิจกรรมกลุ่มตลอดโครงการ (28 ก.ย.–2 ต.ค.) แล้วผู้เข้าร่วมนำ token สะสมไปประมูลของรางวัลในงานเลี้ยงคืนวันที่ 4 (ควบคุมด้วย [auction-app](auction-app/index.html))

**ตัดสินใจแล้ว:** ระบบ e-token นี้จะ**มาแทนที่กลไกแจกมือแบบเดิมทั้งหมด** ไม่ใช่แผนสำรองเฉพาะกรณีกิจกรรมปรับเป็นออนไลน์ — ใช้จริงตลอดโครงการไม่ว่าบรรยายจะจัด onsite หรือ online

## เป้าหมาย (Goals)

- วิทยากร/ผู้ดูแลกลุ่ม กด "แจก" token ให้ผู้เข้าร่วมคนใดก็ได้ทันทีระหว่างบรรยาย ด้วยการกรอกรหัสประจำตัว โดยไม่ต้องพกเหรียญ/สติกเกอร์จริง
- ผู้เข้าร่วมล็อกอินดูยอด token สะสมของตัวเองได้ตลอดโครงการ
- มีประวัติการแจก/หักที่ตรวจสอบย้อนหลังได้ (ใครทำรายการ, กับใคร, เมื่อไหร่, กี่ token, เหตุผล/รายการอะไร)
- คืนวันที่ 4 เจ้าหน้าที่หักยอด token ของผู้ชนะประมูลออกจากระบบโดยตรง พร้อมระบุรายการของรางวัลที่แลกไป (**ตัดสินใจแล้ว:** ไม่เชื่อม auction-app แบบอัตโนมัติ — เจ้าหน้าที่กรอกหักด้วยมือแทน)

## ไม่ใช่เป้าหมาย (Non-goals ในรอบนี้)

- ไม่ใช่ระบบประมูลจริง (นั่นคือหน้าที่ของ auction-app ที่มีอยู่แล้ว)
- ไม่ใช่ระบบจ่าย/แลกเงินจริงหรือของรางวัลจริงโดยตรง
- ไม่ทำระบบสมัครสมาชิก/ลงทะเบียนผู้เข้าร่วมใหม่ (รายชื่อผู้เข้าร่วมมีอยู่แล้วจากการยืนยันเข้าร่วมก่อนหน้านี้)

## ผู้ใช้งาน (Users)

| บทบาท | ทำอะไรได้ |
|---|---|
| **ผู้เข้าร่วม (Trainer ในกิจกรรม)** | ล็อกอินด้วย username/password ที่ระบบสร้างไว้ให้ล่วงหน้า → ดูยอด token สะสมของตัวเอง + ประวัติที่ได้รับ/ถูกหัก → ควรเปลี่ยนรหัสผ่านเองภายหลัง |
| **วิทยากร/ผู้ดูแลกลุ่ม** | ล็อกอิน → กรอกรหัสประจำตัวผู้เข้าร่วม (แจกให้ใครก็ได้ ไม่จำกัดเฉพาะกลุ่มตัวเอง) → กด "แจก" token พร้อมระบุจำนวน/เหตุผล → ยอดที่แจกได้ถูกหักจาก **โควตา token ในมือของตัวเอง** (ดูรายละเอียดด้านล่าง) |
| **ผู้ดูแลระบบ (แอดมิน)** | ดูภาพรวมทั้งหมด, ตั้งค่าโควตา token ของวิทยากรแต่ละคน, แก้ไข/ยกเลิกรายการแจก-หักที่ผิดพลาด, สร้าง/รีเซ็ตบัญชีผู้ใช้, export ข้อมูล, **และเป็นผู้กด "หัก" token คืนวันที่ 4** (กรอกรหัสประจำตัวผู้ชนะประมูล + ระบุรายการของรางวัลที่แลกไป แทนการเชื่อมกับ auction-app แบบอัตโนมัติ — **ตัดสินใจแล้ว:** ไม่มีบทบาท "เจ้าหน้าที่" แยกต่างหาก แอดมินทำหน้าที่นี้เอง) |

## Flow หลัก

1. **แจก token:** วิทยากรล็อกอิน → กรอกรหัสประจำตัวผู้เข้าร่วม → เลือกจำนวน (ปุ่มพื้นฐาน 1/3/5/10/15 หรือระบุเอง) → เลือกเหตุผล (ปุ่มพื้นฐานหรือพิมพ์เอง) → กดยืนยัน → ระบบบันทึกทันที และหักออกจากโควตา token ของวิทยากรคนนั้น
2. **หัก token (คืนประมูล):** แอดมินล็อกอิน → กรอกรหัสประจำตัวผู้ชนะประมูล → ระบุจำนวนที่หัก + รายการของรางวัลที่แลกไป → กดยืนยัน → ระบบบันทึกเป็นรายการติดลบในประวัติ
3. **ดูยอดสะสม:** ผู้เข้าร่วมล็อกอินด้วย username/password → หน้าแรกแสดงยอดรวม + รายการประวัติทั้งแจกและหัก (วันที่, เหตุผล/รายการ, ผู้ทำรายการ, จำนวน) เรียงใหม่→เก่า
4. **เปลี่ยนรหัสผ่าน (แนะนำ ไม่บังคับ):** ผู้เข้าร่วมเข้าหน้าตั้งค่าบัญชี → เปลี่ยนรหัสผ่านจากรหัสชั่วคราวเป็นรหัสของตัวเอง

## ระบบโควตา token ของวิทยากร

- วิทยากร/ผู้ดูแลกลุ่มแต่ละคนมี **token ในมือจำกัดจำนวน** (คล้ายงบให้แจก ไม่ใช่แจกได้ไม่อั้น) — ทุกครั้งที่กด "แจก" ยอดจะถูกหักออกจากโควตาของวิทยากรคนนั้นเอง ไม่ใช่หักจากส่วนกลาง
- **โควตาเริ่มต้น: 50 token ต่อวิทยากรหนึ่งคน** — เป็นค่าเริ่มต้นเดียวกันทุกคน แต่แอดมินปรับ (เพิ่ม/ลด) เป็นรายบุคคลภายหลังได้ผ่านหน้าจัดการแอดมิน
- ปุ่มจำนวนพื้นฐานสำหรับแจก: **1, 3, 5, 10, 15** + ปุ่ม "ระบุจำนวนเอง"
- ปุ่มเหตุผลพื้นฐาน: **"ตอบคำถาม"**, **"ร่วมกิจกรรม"** + ปุ่ม "ระบุเหตุผลเอง"

## Backend / Auth — ตัดสินใจแล้ว

- **Firebase project `tft-e-token`** (Firebase Authentication + Firestore) — เป็น backend กลางของระบบนี้ทั้งหมด
- **Auth: username/password แบบสร้างล่วงหน้า** — แอดมินสร้างบัญชีให้ผู้เข้าร่วม/วิทยากรทุกคนไว้ก่อนงาน พร้อม**รหัสผ่านชั่วคราว** แจกให้แต่ละคนรู้เฉพาะของตัวเอง ผู้ใช้ล็อกอินครั้งแรกด้วยรหัสชั่วคราว แล้ว **แนะนำ** ให้เปลี่ยนเป็นรหัสของตัวเองภายหลัง (ไม่บังคับเปลี่ยนก่อนใช้งาน)
- **Username:** เก็บจริงเป็น `tft` + เลขสองหลัก (เช่น `tft01`, `tft02`, ... `tft80`) เรียงต่อเนื่องทุกบัญชี แต่**ทุกช่องกรอกในหน้าเว็บ (ล็อกอิน/แจก/หัก/สร้างบัญชี) ให้ผู้ใช้พิมพ์แค่เลข 2 หลักท้ายเท่านั้น** (เช่น พิมพ์ `23` ไม่ต้องพิมพ์ `tft23`) ระบบเติม prefix ให้เองที่เดียวใน `shared/firebase-init.js` (`toFullUsername`/`toShortCode`) — ผู้ใช้ไม่เคยเห็น/พิมพ์ prefix เลย
- **Password ชั่วคราว:** ตัวเลขสุ่มล้วน 6 หลัก — **ตัวเลขนี้แก้ให้สั้นกว่านี้ไม่ได้** เพราะ Firebase Authentication บังคับรหัสผ่านอย่างน้อย 6 ตัวอักษรเป็นค่าต่ำสุดของแพลตฟอร์ม (ตั้งค่าให้สั้นกว่านี้ไม่ได้แม้จะปรับ password policy ใน Console ก็ตาม)
- **สลิปแจกรหัสผ่าน:** พิมพ์แยกทีละคน มีข้อมูล **เลขที่ (ลำดับ), ชื่อ-นามสกุล, username, password** + ข้อความเตือนเรื่องความปลอดภัย (ห้ามใช้รหัสผ่านส่วนตัว/ใช้ซ้ำกับบัญชีอื่น)
- **ต้องสื่อสารกับผู้เข้าร่วมให้ชัดเจนตอนแจกรหัสชั่วคราว:** "นี่คือรหัสผ่านชั่วคราว **ห้ามใช้รหัสผ่านส่วนตัว/รหัสที่ใช้ซ้ำกับบัญชีอื่นเด็ดขาด** เพราะระบบนี้ไม่ได้ทำระบบรักษาความปลอดภัยระดับสูง (ไม่มี 2FA, ไม่มี rate-limit เข้มงวด ฯลฯ)" — ข้อความนี้ต้องปรากฏให้เห็นชัดทั้งตอนแจกรหัสและในหน้าล็อกอิน/หน้าเปลี่ยนรหัสผ่านของแอป

**ข้อควรระวังด้านความปลอดภัยที่ต้องทำ (ไม่ใช่ทางเลือก) เพราะ auth เบากว่าระบบทั่วไป:**
- ต้องตั้ง **Firestore Security Rules** ให้ผู้เข้าร่วมอ่านได้เฉพาะยอด/ประวัติของตัวเอง และ **ห้ามเขียนแก้ยอด token ของตัวเองได้เด็ดขาด** — การเขียนยอดต้องทำผ่านฝั่งวิทยากร/แอดมินเท่านั้น (เช่น จำกัดสิทธิ์เขียนด้วย custom claims บทบาท `trainer`/`admin`) ไม่เช่นนั้นผู้เข้าร่วมที่เปิด dev tools จะแก้ยอดตัวเองได้ตรง ๆ ผ่าน Firestore client SDK
- `firebaseConfig` (apiKey ฯลฯ) ฝังในโค้ดฝั่ง client ได้ตามปกติ (ไม่ใช่ความลับ) — ตัวที่ป้องกันจริงคือ Security Rules ไม่ใช่การซ่อน config

## ขอบเขต (In / Out of scope)

**In scope**
- ระบบล็อกอินด้วย username/password ที่สร้างไว้ล่วงหน้า (Firebase Auth) + หน้าเปลี่ยนรหัสผ่าน
- หน้าจัดการสำหรับวิทยากร: แจก token ด้วยรหัสประจำตัวผู้เข้าร่วม (ปุ่มจำนวน/เหตุผลพื้นฐาน + ระบุเอง), ดูโควตา token คงเหลือของตัวเอง, ดูประวัติที่ตัวเองแจกไป
- หน้าแสดงยอดสำหรับผู้เข้าร่วม: ยอดรวม + ประวัติแจก/หักทั้งหมด
- หน้าแอดมิน: ภาพรวมยอดทุกคน, ตั้งค่าโควตา token ของวิทยากรแต่ละคน, หัก token พร้อมระบุรายการที่แลกไป (ใช้คืนประมูล), แก้ไข/ยกเลิกรายการแจก-หัก (เฉพาะแอดมินเท่านั้นที่แก้ไขย้อนหลังได้), สร้าง/รีเซ็ตบัญชีผู้ใช้, export CSV
- เก็บประวัติแบบ audit trail (แก้ไขย้อนหลังได้แต่ต้องมีร่องรอย ไม่ลบทิ้งเงียบ ๆ)

**Out of scope (รอบนี้)**
- การเชื่อมต่อ/ซิงก์กับ auction-app แบบอัตโนมัติ (**ตัดสินใจแล้ว:** ไม่ทำ — ใช้เจ้าหน้าที่กรอกหักด้วยมือแทน ดูหัวข้อ Flow ด้านบน)
- แจ้งเตือนแบบ push/LINE Notify เมื่อได้รับ token (ยังไม่ได้ขอ)
- แอปมือถือแยก (ใช้เว็บ responsive พอ)

## ข้อจำกัด / บริบทการใช้งานที่ต้องคำนึงถึง

- **ต้องมีอินเทอร์เน็ตและ backend กลาง** — ต่างจากเครื่องมืออื่นในโปรเจกต์นี้ (index.html คู่มือ, auction-app) ที่ตั้งใจให้ทำงานได้แบบ static/offline ไม่มี backend เพราะระบบนี้ต้องให้วิทยากรหลายคนแจก token แบบเรียลไทม์ และผู้เข้าร่วมต้องล็อกอินดูยอดพร้อมกันได้ จึงเลี่ยง backend ไม่ได้
- โน้ตบุ๊ก/มือถือของวิทยากรแต่ละคนต้องต่อเน็ตได้ระหว่างบรรยาย (auction-app เดิมเคยระบุว่าสถานที่จัดงานสัญญาณเน็ตไม่แน่นอน — ควรตรวจสอบสัญญาณ ณ ห้องบรรยายจริงก่อนวันงาน ไม่ใช่แค่ที่งานเลี้ยง)
- ผู้เข้าร่วมมีทั้งหมด 7 กลุ่มสาขา (ตามโครงสร้างเดิมของ TFT)
- **จำนวนผู้ใช้จริง:** ผู้เข้าร่วม **60 คน** (`tft01`–`tft60`, สร้างบัญชีจริงแล้ว), วิทยากร/ที่ปรึกษาประจำกลุ่ม **24 คน** (`tft61`–`tft84`, สร้างบัญชีจริงแล้ว — มากกว่าตัวเลขประมาณการเดิม 20 คน เพราะไล่ตามรายชื่อจริงใน `docs/02-agenda.md` ทั้งวิทยากรบรรยายและวิทยากรที่ปรึกษาประจำกลุ่ม 7 กลุ่ม), รวม admin 1 คน (`tft00`) — ทั้งหมด 85 บัญชี

## คำถามเปิด

ไม่มีคำถามเปิดค้างแล้ว — ทุกข้อได้คำตอบครบตามหัวข้อต่าง ๆ ด้านบน พร้อมเริ่มออกแบบ/สร้างจริง

**สิ่งที่ยังต้องเตรียมก่อนวันงาน (ไม่ใช่คำถามเชิงสถาปัตยกรรม แต่เป็นงาน operational ที่ต้องทำ):**
- ~~รายชื่อ+รหัสประจำตัวผู้เข้าร่วม 60 คน และวิทยากร 24 คน แบบสมบูรณ์~~ — **เสร็จแล้ว** สร้างบัญชีจริง `tft01`–`tft84` ครบทั้งหมดแล้ว (ผู้เข้าร่วมจากไฟล์ CSV ที่ผู้จัดส่งมา, วิทยากรไล่จากรายชื่อใน `docs/02-agenda.md`)
- ตรวจสอบสัญญาณอินเทอร์เน็ต ณ ห้องบรรยายจริง (ไม่ใช่แค่ห้องงานเลี้ยง) ก่อนวันงาน
- ตั้งค่า Firestore Security Rules ตามที่ระบุไว้ในหัวข้อ Backend/Auth ก่อนใช้งานจริง

## ความสัมพันธ์กับส่วนอื่นของโปรเจกต์

แอปนี้ควรแยกเป็นโปรเจกต์ย่อยของตัวเอง (คล้าย [auction-app/](auction-app/)) ไม่ใช้ดีไซน์ระบบเดียวกับเว็บคู่มือหลัก ([DESIGN.md](DESIGN.md) สงวนไว้เฉพาะเว็บคู่มือสาธารณะ) — ตั้งชื่อโฟลเดอร์และตัดสินใจดีไซน์แยกต่างหากเมื่อคำถามเปิดด้านบนได้คำตอบครบแล้ว
